{
  "name": "smilecraft-carousel-studio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local-first dental clinic carousel studio with a Codex CLI writing gateway",
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
