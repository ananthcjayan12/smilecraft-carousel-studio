import { TOPICS, TEMPLATES, ROLES, newProject, starterSlides, captionFallback } from './data.js';
import { optimizedImage, finalArtworkBlob } from './canvas.js';
import { makeZip, downloadBlob } from './zip.js';
import { DESIGN_SYSTEMS, blobToDataUrl, slideReference } from './design-systems.js';
import { installTemplatePack } from './template-pack.js';

const el = document.querySelector('#app');
const state = { view: 'home', project: newProject(), index: 0, busy: '', status: {}, agyModels: [], projects: [], captionTab: 'instagram', toastTimer: null, renderNonce: 0, saving: false, changeTimer: null, correction: '', includeCaptions: true, includeProject: true, installedSystems: new Set(), batchDone: 0, batchTotal: 0, packProgress: '' };
const e = value => String(value ?? '').replace(/[&<>"']/g, ch => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[ch]);
const text = s => e(s).replace(/\n/g, '<br>');
const $ = selector => document.querySelector(selector);
const allTemplates = () => [...TEMPLATES, ...DESIGN_SYSTEMS.filter(t => state.installedSystems.has(t.id)), ...(state.project.customTemplates || [])];
const activeTemplate = () => allTemplates().find(t => t.id === state.project.template) || TEMPLATES[0];
const approvedCount = () => state.project.slides.filter(s => s.approved).length;
const artworkCount = () => state.project.slides.filter(s => s.artwork).length;
const current = () => state.project.slides[state.index];
const IMAGE_MODELS = {
  openai: [['gpt-image-2', 'GPT Image 2'], ['gpt-image-2.5-sunburst', 'GPT Image 2.5 Sunburst'], ['gpt-image-2.5-flare', 'GPT Image 2.5 Flare'], ['gpt-image-1', 'GPT Image 1'], ['gpt-image-1-mini', 'GPT Image 1 mini']],
  gemini: [['gemini-3.1-flash-image', 'Gemini 3.1 Flash Image (Nano Banana 2)'], ['gemini-3-pro-image', 'Gemini 3 Pro Image (Nano Banana Pro)'], ['gemini-3.1-flash-lite-image', 'Gemini 3.1 Flash Lite Image'], ['gemini-2.5-flash-image', 'Gemini 2.5 Flash Image']],
  codex: [['gpt-5.6-sol', 'GPT-5.6 Sol'], ['gpt-5.6-terra', 'GPT-5.6 Terra'], ['gpt-5.6-luna', 'GPT-5.6 Luna']],
  antigravity: [['gemini-3.8-flash-high', 'Gemini 3.8 Flash (High)'], ['gemini-3.8-flash-medium', 'Gemini 3.8 Flash (Medium)'], ['gemini-3.8-flash-low', 'Gemini 3.8 Flash (Low)'], ['gemini-3.1-pro-high', 'Gemini 3.1 Pro (High)'], ['claude-sonnet-4-6', 'Claude Sonnet 4.6'], ['claude-opus-4-6-thinking', 'Claude Opus 4.6 (Thinking)'], ['gpt-oss-120b-medium', 'GPT-OSS 120B (Medium)']],
};
const WRITING_MODELS = {
  codex: [['gpt-5.6-sol', 'GPT-5.6 Sol'], ['gpt-5.6-terra', 'GPT-5.6 Terra'], ['gpt-5.6-luna', 'GPT-5.6 Luna']],
  openai: [['gpt-5.6-sol', 'GPT-5.6 Sol'], ['gpt-5.6-terra', 'GPT-5.6 Terra'], ['gpt-5.6-luna', 'GPT-5.6 Luna']],
  gemini: [['gemini-3.1-pro', 'Gemini 3.1 Pro'], ['gemini-3.8-flash', 'Gemini 3.8 Flash'], ['gemini-2.5-flash', 'Gemini 2.5 Flash']],
  antigravity: [['gemini-3.8-flash-high', 'Gemini 3.8 Flash (High)'], ['gemini-3.8-flash-medium', 'Gemini 3.8 Flash (Medium)'], ['claude-sonnet-4-6', 'Claude Sonnet 4.6'], ['claude-opus-4-6-thinking', 'Claude Opus 4.6 (Thinking)']],
  claude: [['claude-sonnet-4-6', 'Claude Sonnet 4.6'], ['claude-opus-4-6', 'Claude Opus 4.6'], ['claude-haiku-4-5-20251001', 'Claude Haiku 4.5']],
};
function providerModels(provider, kind = 'writing') { if (provider === 'antigravity' && state.agyModels.length) return state.agyModels.map(item => [item.id, `${item.label} · ${item.id}`]); return (kind === 'image' ? IMAGE_MODELS : WRITING_MODELS)[provider] || []; }
const modelSelect = (options, selected, attribute) => `<select class="control" ${attribute}>${options.map(([id, label]) => `<option value="${e(id)}" ${id === selected ? 'selected' : ''}>${e(label)}</option>`).join('')}</select>`;
function providerState(provider) { if (provider?.available) return 'ready'; if (provider?.installed && provider?.authenticated === false) return 'login required'; return 'unavailable'; }
function writingProviderOptions() { const providers = state.status.textProviders || {}; return [['codex','Codex CLI'],['openai','OpenAI API'],['gemini','Gemini API'],['antigravity','Antigravity CLI (agy)'],['claude','Claude API']].map(([id, label]) => `<option value="${id}" ${state.project.generation?.writingProvider === id ? 'selected' : ''}>${label} · ${providerState(providers[id])}</option>`).join(''); }
const section = (title, sub = '') => `<div class="header-row"><div><h1 class="page-title">${e(title)}</h1><p class="subtitle">${e(sub)}</p></div><span class="pill">${approvedCount()} / 5 approved</span></div>`;
function toast(message) { const node = $('#toast'); node.textContent = message; node.classList.add('show'); clearTimeout(state.toastTimer); state.toastTimer = setTimeout(() => node.classList.remove('show'), 5800); }
async function api(url, data, method = data === undefined ? 'GET' : 'POST') {
  const res = await fetch(url, { method, headers: { 'Content-Type': 'application/json' }, body: data === undefined ? undefined : JSON.stringify(data) });
  let json; try { json = await res.json(); } catch { throw new Error(`Request failed (${res.status}).`); }
  if (!res.ok) throw new Error(json.error || `Request failed (${res.status}).`);
  return json;
}
async function listProjects() { try { state.projects = (await api('/api/projects')).projects; } catch (err) { toast(err.message); } }
async function refreshAgyModels(show = false) { try { state.agyModels = (await api('/api/models/agy')).models || []; const g = state.project.generation; if (g?.writingProvider === 'antigravity' && !state.agyModels.some(item => item.id === g.writingModel)) g.writingModel = state.agyModels[0]?.id || ''; if (g?.provider === 'antigravity' && !state.agyModels.some(item => item.id === g.model)) g.model = state.agyModels[0]?.id || ''; if (show) toast(`${state.agyModels.length} AGY models detected.`); render(); } catch (err) { if (show) toast(err.message); } }
async function saveProject(show = false) {
  if (!state.project.topic.trim() || state.saving) return;
  state.saving = true; const savingProject = state.project;
  try {
    const result = await api('/api/projects', savingProject);
    if (state.project === savingProject) { savingProject.id = result.id; savingProject.updatedAt = result.updatedAt; }
    if (show) toast('Project saved locally. You can reopen it from My Projects.');
  } catch (err) { toast(`Could not save: ${err.message}`); } finally { state.saving = false; }
}
function scheduleSave() { clearTimeout(state.changeTimer); state.changeTimer = setTimeout(() => saveProject(), 1200); }
function workflow() { const steps = ['Topic', 'Content', 'Design', 'Review', 'Export']; const stage = Math.max(0, Number(state.project.stage) || 0); return `<div class="stepbar" aria-label="Carousel workflow">${steps.map((name, i) => `<button class="step ${stage === i ? 'active' : i < stage ? 'done' : ''}" data-action="stage" data-index="${i}"><span class="stepnum">${i < stage ? '✓' : i + 1}</span>${name}</button>`).join('')}</div>`; }
function nav() { const navs = [['home', '⌂', 'Home'], ['create', '▧', 'Create Carousel'], ['projects', '▣', 'My Projects'], ['templates', '▦', 'References'], ['brand', '⚙', 'Clinic Settings']]; return `<aside class="side"><div class="logo"><span>♧</span><div class="wordmark">SmileCraft <span style="color:#0d9b9d">AI</span><small style="display:block">Social Media Creator</small></div></div><nav>${navs.map(([key, icon, label]) => `<button class="navitem ${state.view === key ? 'active' : ''}" data-action="nav" data-value="${key}"><span class="navicon">${icon}</span>${label}</button>`).join('')}</nav><div class="side-bottom"><strong>✦ Provider-based studio</strong>Choose the AI provider and model at the writing and image-generation steps.</div></aside>`; }
function shell(content) { const clinic = state.project.brand?.name || 'Dental Clinic'; const labels = { home: 'Dashboard', brand: 'Clinic Settings', templates: 'References', projects: 'My Projects' }; return `<div class="shell">${nav()}<main class="main"><header class="topbar"><div class="crumb">SmileCraft Studio <span> / </span><strong>${state.view === 'create' ? ['Topic', 'Content', 'Design', 'Review', 'Export'][state.project.stage || 0] : labels[state.view] || 'Dashboard'}</strong></div><div class="userpill"><span class="avatar">${e(clinic.charAt(0).toUpperCase())}</span> ${e(clinic)}</div></header>${content}</main></div>`; }
function home() { const tiles = state.projects.slice(0, 4).map(p => `<button class="project-tile" data-action="open-project" data-id="${e(p.id)}"><img src="${e(activeTemplate().img || '/assets/teal-editorial.jpg')}" alt="Carousel example"><div class="tile-info"><b>${e(p.topic || 'Untitled')}</b><small>5 slides · ${e((p.updatedAt || '').slice(0, 10))}</small></div></button>`).join('');
  return `<div class="dashboard-hero"><div><span class="pill">✦ Make scroll-stopping education simple</span><h1 class="headline">Create beautiful<br><b>dental carousels</b><br>in minutes.</h1><p>Approve Malayalam-English copy, choose a visual reference, generate final artwork with your preferred provider, and export an upload-ready ZIP.</p><button class="btn primary big" data-action="new">＋ Create New Carousel →</button></div><div class="dashboard-visual">${['teal-editorial.jpg','friendly-clinic.jpg','premium-dark.jpg'].map(n => `<img src="/assets/${n}" alt="Sample SmileCraft carousel reference">`).join('')}</div></div><div class="feature-row"><div class="feature"><span>✍</span> Approval-first writing</div><div class="feature"><span>◈</span> Reference-led generation</div><div class="feature"><span>▣</span> Separate PNG slides</div><div class="feature"><span>↡</span> Organized ZIP & captions</div></div><div class="header-row"><div><h2 style="font-size:19px;margin:0 0 6px">Recent projects</h2><p class="subtitle">Open a saved project and continue where you left off.</p></div><button class="btn tiny" data-action="nav" data-value="projects">View all →</button></div>${tiles ? `<div class="projects-grid">${tiles}</div>` : `<div class="empty"><div class="emicon">▧</div>No saved projects yet. Create your first carousel to see it here.</div>`}`; }
function topicPage() { const g = state.project.generation || {}, provider = g.writingProvider || 'codex', models = providerModels(provider); return `${section('Create New Carousel', 'Tell us your idea. Build one clean page at a time, with your approval before production.')}${workflow()}<div class="grid-2"><section class="card card-pad"><span class="pill">STEP 01 · YOUR IDEA</span><h2 style="margin-top:18px">What should your carousel be about?</h2><p class="subtitle">Add your topic in Malayalam or English. Your selected provider drafts the five pages; you always review the wording.</p><label class="formlabel" for="topic">Carousel topic or question</label><textarea id="topic" data-field="topic" class="control biginput" maxlength="450" placeholder="ഉദാ: പല്ല് ക്ലീൻ ചെയ്താൽ പല്ലുകൾക്ക് ഇടയിൽ വിടവ് വരുമോ?">${e(state.project.topic)}</textarea><label class="formlabel" for="notes">Extra context (optional)</label><textarea id="notes" data-field="notes" class="control area" placeholder="Target audience, points to cover, style, details to avoid…">${e(state.project.notes || '')}</textarea><div class="subhead">Or choose a popular topic</div><div class="chips">${TOPICS.map(topic => `<button class="chip ${state.project.topic === topic.topic ? 'selected' : ''}" data-action="choose-topic" data-value="${e(topic.key)}">${e(topic.label)}</button>`).join('')}</div><div class="subhead">Copy generation</div><label class="formlabel">Provider</label><select class="control" data-generation="writingProvider">${writingProviderOptions()}</select><label class="formlabel">Model</label>${modelSelect(models, g.writingModel || models[0]?.[0] || '', 'data-generation="writingModel"')}${provider === 'antigravity' ? `<button class="btn tiny" data-action="refresh-agy">Refresh installed AGY models</button>` : ''}<div class="actions"><span class="hint">This provider and model write the five-slide draft and can be changed for rewrites.</span><button class="btn primary" data-action="draft" ${state.busy ? 'disabled' : ''}>${state.busy === 'draft' ? '<span class="spinner"></span> Drafting…' : '✦ Generate slide copy →'}</button></div><div class="note amber" style="margin-top:15px">Choose a ready provider above. API providers use server environment keys; Codex and agy use their local authenticated CLIs.</div><button class="btn soft" data-action="sample" style="margin-top:12px">Start with editable sample →</button></section><aside class="card card-pad"><span class="pill">Your publishing flow</span><div class="intro-art" style="margin-top:13px"><strong>One idea.<br>Five beautiful pages.</strong><div class="hero-icon">🦷</div><div>WRITE → APPROVE → DESIGN → DOWNLOAD</div></div><div class="subhead">What gets created?</div><div class="note">01 Hook · 02 Science · 03 Impact · 04 Action · 05 CTA. Only the last slide has a booking call-to-action.</div></aside></div>`; }
function slideTabs() { return `<div class="slide-tabs">${state.project.slides.map((s, i) => `<button class="slide-tab ${state.index === i ? 'active' : ''} ${s.approved ? 'approved' : ''}" data-action="select-slide" data-index="${i}"><strong>Slide ${i + 1}</strong>${ROLES[i]}</button>`).join('')}</div>`; }
function previewImg(i, klass = '') { const slide = state.project.slides[i]; const src = slide?.artwork || activeTemplate().img; return `<img class="${klass} ${slide?.artwork ? '' : 'reference-only'} ${!slide?.artwork && activeTemplate().master ? 'master-preview' : ''}" alt="${slide?.artwork ? 'Generated carousel' : 'Template reference for'} slide ${i + 1}" src="${e(src)}" loading="lazy">`; }
function contentPage() { const s = current(), g = state.project.generation || {}, provider = g.writingProvider || 'codex', models = providerModels(provider); return `${section('Review your slide copy', 'Edit every page, request a specific correction from your selected provider, and approve all five before generation.')}${workflow()}<div class="card card-pad">${slideTabs()}<div class="two-col-editor"><section><div class="meta-row"><h2>Slide ${state.index + 1} — ${e(s.role)}</h2><span class="pill ${s.approved ? 'green' : 'amber'}">${s.approved ? '✓ Approved' : '○ Needs approval'}</span></div><label class="formlabel" for="heading">Headline</label><textarea id="heading" data-field="heading" class="control area textarea-small" maxlength="100">${e(s.heading)}</textarea><label class="formlabel" for="body">Supporting text · keep it short</label><textarea id="body" data-field="body" class="control area textarea-small" maxlength="240">${e(s.body)}</textarea><label class="formlabel" for="visualPrompt">Image idea for this slide</label><textarea id="visualPrompt" data-field="visualPrompt" class="control area textarea-small" maxlength="550">${e(s.visualPrompt)}</textarea><div class="subhead">Rewrite generation</div><label class="formlabel">Provider</label><select class="control" data-generation="writingProvider">${writingProviderOptions()}</select><label class="formlabel">Model</label>${modelSelect(models, g.writingModel || models[0]?.[0] || '', 'data-generation="writingModel"')}${provider === 'antigravity' ? `<button class="btn tiny" data-action="refresh-agy">Refresh installed AGY models</button>` : ''}<label class="formlabel" for="correction">Ask for a correction (optional)</label><textarea id="correction" class="control area textarea-small" placeholder="ഉദാ: ഇത് കൂടുതൽ ലളിതമായ മലയാളത്തിൽ എഴുതൂ…">${e(state.correction)}</textarea><div class="actions"><button class="btn soft" data-action="revise" ${state.busy ? 'disabled' : ''}>${state.busy === 'revise' ? 'Rewriting…' : '✦ Rewrite this slide'}</button><button class="btn ${s.approved ? '' : 'primary'}" data-action="approve">${s.approved ? '↶ Reopen slide' : '✓ Approve this slide'}</button></div><p class="hint">Any text edit or AI revision deletes outdated artwork and reopens the slide. No booking CTA on slides 1–4.</p></section><aside><div class="editor-preview preview-frame">${previewImg(state.index)}</div><div class="note" style="margin-top:15px">${s.artwork ? 'Generated final artwork. Editing approved content will invalidate it.' : 'Template reference only. Approve the copy, then generate the final slide in Design.'}</div></aside></div><div class="actions"><button class="btn" data-action="stage" data-index="0">← Topic</button><button class="btn primary" data-action="stage" data-index="2">Choose a reference & generate →</button></div></div>`; }
function templateCard(t) { return `<button class="template-card ${t.master ? 'master-template-card' : ''} ${state.project.template === t.id ? 'selected' : ''}" data-action="choose-template" data-value="${e(t.id)}"><img src="${e(t.img)}" alt="${e(t.name)} reference appearance"><span class="check">${state.project.template === t.id ? '●' : '○'}</span><strong>${e(t.name)}</strong><small>${e(t.kind)} · ${e(t.layout)}</small></button>`; }
function providerOptions() { const providers = state.status.imageProviders || {}; return [['openai','OpenAI API'],['gemini','Gemini API'],['codex','Codex CLI'],['antigravity','Antigravity CLI (agy)']].map(([id, label]) => `<option value="${id}" ${state.project.generation?.provider === id ? 'selected' : ''}>${label} · ${providerState(providers[id])}</option>`).join(''); }
function designPage() { const s = current(), provider = state.project.generation?.provider || 'openai', models = providerModels(provider, 'image'), model = state.project.generation?.model || models[0]?.[0] || ''; return `${section('Generate final carousel artwork', 'Select one master design to guide the typography, colors, placement and final-slide CTA across all five AI-generated slides.')}${workflow()}<div class="card card-pad"><div class="header-row"><div><h2>3. Select a visual reference</h2><p class="subtitle">Each master template defines five coordinated slide layouts. The model receives both the full design board and an enlarged reference of the matching slide, plus your approved copy and optional exact logo.</p></div><span class="pill">${e(activeTemplate().name)}</span></div><div class="template-grid">${allTemplates().map(templateCard).join('')}</div><div class="note" style="margin-top:16px"><b>10 AI-designed five-slide master templates</b> · Download the companion ZIP and import it once on this computer. Original full-quality artwork is retained. Only slide 5 includes the phone, location and booking CTA.</div><label class="file-upload" style="margin-top:12px"><input type="file" accept=".zip,application/zip" data-file-action="template-pack"><span>＋ Install 10 master designs from ZIP<br><small>One-time local import · No image generation needed</small></span></label><p class="hint" id="pack-progress">${e(state.packProgress || `${state.installedSystems.size} / 10 master designs installed`)}</p><label class="file-upload" style="margin-top:16px"><input type="file" accept="image/png,image/jpeg,image/webp" data-file-action="template"><span>＋ Upload a custom template reference<br><small>Used as visual guidance by the selected image provider.</small></span></label></div><div class="grid-side" style="margin-top:18px"><section class="card card-pad"><h2>Generate publication-ready slides</h2><div class="note">Parallel image runners: ${e(state.status.providerConcurrency?.[provider] || 1)} for ${e(provider)} (shared server queue). API providers default to 5; CLIs default to 2; tune IMAGE_PARALLEL_* per your provider quota and machine.</div><p class="hint" id="batch-progress">${state.busy === 'all-images' ? `Generated ${state.batchDone} / ${state.batchTotal} pending slides…` : ''}</p><p class="subtitle">Only approved content can be generated. Provider calls may use paid quota.</p>${slideTabs()}<label class="formlabel">Image provider</label><select class="control" data-generation="provider">${providerOptions()}</select><label class="formlabel">${e(state.status.imageProviders?.[provider]?.label || provider)} model</label>${modelSelect(models, model, 'data-generation="model"')}${provider === 'antigravity' ? `<button class="btn tiny" data-action="refresh-agy">Refresh installed AGY models</button>` : ''}<p class="hint">The provider and model selected here create the final carousel images. OpenAI and Gemini use API keys; Codex and Antigravity use their locally authenticated CLIs.</p><div class="note ${state.status.imageProviders?.[provider]?.available ? '' : 'amber'}" style="margin-top:15px">${state.status.imageProviders?.[provider]?.available ? `${e(state.status.imageProviders[provider].label)} is ready.` : `${e(state.status.imageProviders?.[provider]?.label || provider)} is not configured or detected.`}</div><div class="actions"><button class="btn soft" data-action="generate-image" ${state.busy || !s.approved ? 'disabled' : ''}>${state.busy === 'image' ? '<span class="spinner"></span> Generating…' : s.artwork ? '↻ Regenerate this slide' : '✦ Generate this slide'}</button><button class="btn primary" data-action="generate-all" ${state.busy || approvedCount() !== 5 || artworkCount() === 5 ? 'disabled' : ''}>${state.busy === 'all-images' ? '<span class="spinner"></span> Generating set…' : `Generate missing slides (${5 - artworkCount()})`}</button></div><p class="hint">Generated text can contain mistakes. Verify every Malayalam and English word in Review.</p></section><aside class="card card-pad"><h2>${s.artwork ? 'Generated final slide' : 'Selected reference'}</h2><div class="editor-preview preview-frame" style="margin-top:15px">${previewImg(state.index)}</div><div class="actions"><button class="btn tiny" data-action="clear-artwork" ${s.artwork ? '' : 'disabled'}>Discard artwork</button><button class="btn primary" data-action="stage" data-index="3">Review ${artworkCount()} / 5 →</button></div></aside></div>`; }
function reviewPage() { return `${section('Review generated artwork', 'Inspect every generated image and verify all text before export.')}${workflow()}<div class="card card-pad"><div class="meta-row"><h2>4. Final review</h2><span class="pill ${artworkCount() === 5 ? 'green' : 'amber'}">${artworkCount() === 5 ? 'All artwork generated' : `${5 - artworkCount()} slide(s) still need artwork`}</span></div><div class="review-strip">${state.project.slides.map((s, i) => `<button class="review-slide ${i === state.index ? 'active' : ''}" data-action="select-slide" data-index="${i}">${previewImg(i)}<small>0${i + 1} · ${e(s.role)} ${s.artwork ? '✓' : 'reference only'}</small></button>`).join('')}</div><div class="review-layout" style="margin-top:21px"><div class="big-preview preview-frame">${previewImg(state.index)}</div><div><h2>Slide ${state.index + 1} · ${e(current().role)}</h2><p class="subtitle" style="margin-bottom:15px">${e(current().heading)}</p><p style="font-size:13px;color:#637f87;line-height:1.75">${e(current().body)}</p><div class="note ${current().artwork ? 'amber' : ''}" style="margin-top:20px">${current().artwork ? `Generated with <b>${e(current().artworkProvider || 'AI')}</b>. Compare every rendered word with the approved heading/body; image models can make typography mistakes.` : 'This is only the selected template reference. Generate this slide before export.'}</div><div class="actions"><button class="btn soft" data-action="stage" data-index="1">✎ Edit approved copy</button><button class="btn soft" data-action="stage" data-index="2">✦ Regenerate artwork</button></div><div class="actions"><button class="btn tiny" data-action="download-one" ${current().artwork ? '' : 'disabled'}>↡ Download this PNG</button><button class="btn primary" data-action="stage" data-index="4">Export options →</button></div></div></div></div>`; }
function captionField() { const p = state.project; const fallback = captionFallback(p); const key = state.captionTab; const val = p[key] || fallback[key]; return `<div class="caption-tabs">${[['instagram','Instagram caption'],['youtubeTitle','YouTube title'],['youtubeDescription','YouTube description']].map(([k,label]) => `<button class="btn tiny ${key === k ? 'active' : ''}" data-action="caption-tab" data-value="${k}">${label}</button>`).join('')}</div><textarea class="control captionbox" data-field="${key}" aria-label="${e(key)}">${e(val)}</textarea><div class="actions"><span class="hint">${p[key] ? 'Edited/generated copy' : 'Auto-composed from your current slides; review before publishing.'}</span><button class="btn tiny" data-action="copy-caption">Copy ${e(key)}</button></div>`; }
function exportPage() { const ready = approvedCount() === 5 && artworkCount() === 5; return `${section('Export your carousel', 'Download the five generated slides, captions and editable project backup.')}${workflow()}<div class="grid-side"><section class="card card-pad"><h2>5. Choose your deliverables</h2><p class="subtitle">Each PNG comes from the selected image provider and is normalized to 1080 × 1350.</p><div class="export-grid"><button class="export-option selected" data-action="noop"><span class="bigicon">▧</span><b>Images (ZIP)</b><small>5 generated final PNGs, 1080 × 1350</small></button><button class="export-option ${state.includeCaptions ? 'selected' : ''}" data-action="toggle-captions"><span class="bigicon">♧</span><b>Social captions ${state.includeCaptions ? '✓' : ''}</b><small>Instagram copy and YouTube title + description</small></button><button class="export-option ${state.includeProject ? 'selected' : ''}" data-action="toggle-project"><span class="bigicon">▤</span><b>Editable project ${state.includeProject ? '✓' : ''}</b><small>Importable JSON with generated artwork and references</small></button><button class="export-option" data-action="download-one"><span class="bigicon">↡</span><b>Single PNG</b><small>Download the generated slide selected in Review</small></button></div><div class="note ${ready ? '' : 'amber'}">${ready ? 'All five approved slides have generated artwork and are ready to export.' : `Export needs five approved slides and five generated artworks. Current artwork: ${artworkCount()} / 5.`}</div><div class="actions right"><button class="btn" data-action="stage" data-index="3">← Review</button><button class="btn primary big" data-action="export" ${!ready || state.busy ? 'disabled' : ''}>${state.busy === 'export' ? '<span class="spinner"></span> Preparing PNGs…' : '↡ Download generated ZIP'}</button></div></section><aside class="card card-pad"><h2>Social copy</h2><p class="subtitle">Edit the copy before export. It will be saved in plain-text files inside the ZIP.</p>${captionField()}</aside></div>`; }
function brandPage() { const b = state.project.brand; return `<div class="header-row"><div><h1 class="page-title">Clinic Settings</h1><p class="subtitle">These exact details guide generated copy and final slide artwork.</p></div><button class="btn primary" data-action="save">Save clinic settings</button></div><div class="grid-side"><section class="card card-pad"><h2>Your clinic identity</h2><label class="formlabel">Clinic / brand name</label><input class="control" data-brand="name" maxlength="80" value="${e(b.name)}" placeholder="Your Dental Clinic"><label class="formlabel">Phone number</label><input class="control" data-brand="phone" type="tel" inputmode="tel" maxlength="40" value="${e(b.phone || '')}" placeholder="e.g. +91 98765 43210"><p class="hint">Leave this blank to omit the number. Providers are instructed never to invent one.</p><label class="formlabel">Brand subtitle</label><input class="control" data-brand="tagline" maxlength="40" value="${e(b.tagline)}"><div class="setting-grid"><div><label class="formlabel">Primary brand color</label><input type="color" data-brand="primary" value="${e(b.primary)}"><span class="hint">${e(b.primary)}</span></div><div><label class="formlabel">Accent brand color</label><input type="color" data-brand="accent" value="${e(b.accent)}"><span class="hint">${e(b.accent)}</span></div></div><label class="formlabel">Clinic location (CTA slide 5 only)</label><input class="control" data-brand="location" maxlength="100" value="${e(b.location || '')}" placeholder="Sreenarayanapuram, Ezhupunna"><label class="formlabel">Optional logo reference</label><label class="file-upload"><input type="file" accept="image/png,image/jpeg,image/webp" data-file-action="logo">＋ Upload your logo (prefer a simple transparent PNG)</label><p class="hint">The logo is supplied as a separate reference image to the selected provider.</p><div class="actions"><button class="btn tiny" data-action="clear-logo">Clear logo</button><button class="btn primary" data-action="stage" data-index="2">Generate branded slides →</button></div></section><aside class="card card-pad"><h2>${current().artwork ? 'Generated artwork' : 'Current visual reference'}</h2><div class="editor-preview preview-frame" style="margin-top:13px">${previewImg(state.index)}</div><div class="note amber" style="margin-top:15px">Changing clinic identity, colors or logo invalidates existing generated artwork so outdated branding cannot be exported.</div></aside></div>`; }
function projectsPage() { return `<div class="header-row"><div><h1 class="page-title">My Projects</h1><p class="subtitle">Locally saved in the app's <code>storage/projects</code> folder on this computer.</p></div><div class="actions" style="margin:0"><label class="btn" for="project-import">↑ Import project JSON</label><input type="file" accept="application/json,.json" id="project-import" data-file-action="import" hidden><button class="btn primary" data-action="new">＋ New carousel</button></div></div><div class="project-list">${state.projects.length ? state.projects.map(p => `<div class="project-row"><div class="project-emoji">🦷</div><div class="rowinfo"><strong>${e(p.topic)}</strong><small>Updated ${e((p.updatedAt || '').slice(0, 16).replace('T', ' '))} · ${e(p.template)}</small></div><span class="mini-stat">${p.approved}/5 approved</span><button class="btn tiny primary" data-action="open-project" data-id="${e(p.id)}">Open →</button><button class="btn tiny danger" data-action="delete-project" data-id="${e(p.id)}">Delete</button></div>`).join('') : `<div class="empty"><div class="emicon">▣</div>No saved projects yet.</div>`}</div>`; }
function templateLibraryPage() { return `<div class="header-row"><div><h1 class="page-title">Template References</h1><p class="subtitle">Choose one five-slide design system for a consistent Malayalam-English informational carousel.</p></div><button class="btn primary" data-action="nav" data-value="create">Open carousel editor →</button></div><div class="card card-pad"><div class="template-grid">${allTemplates().map(templateCard).join('')}</div><div class="note" style="margin-top:20px">Use the ZIP installer in the Design step to add the ten full-quality AI master boards to the library. Each is reused on every page of a carousel, with the matching panel supplying layout guidance. The model still generates the final imagery and lettering; review the exact Malayalam, clinic details and medical content.</div></div>`; }
function render() {
  const stage = Number(state.project.stage) || 0;
  let content = state.view === 'home' ? home() : state.view === 'projects' ? projectsPage() : state.view === 'brand' ? brandPage() : state.view === 'templates' ? templateLibraryPage() : stage === 0 ? topicPage() : stage === 1 ? contentPage() : stage === 2 ? designPage() : stage === 3 ? reviewPage() : exportPage();
  el.innerHTML = shell(content);
}
async function showView(view) { if (view === 'create') state.project.stage = state.project.stage ?? 0; if (view === 'projects' || view === 'home') await listProjects(); state.view = view; render(); }
function gotoStage(number) { state.project.stage = Math.max(0, Math.min(4, Number(number))); state.view = 'create'; if (state.project.stage === 4 && (approvedCount() !== 5 || artworkCount() !== 5)) toast('Full export requires five approved slides and five generated artworks. You can still preview this screen.'); scheduleSave(); render(); }
function setTopic(topic, key = '') { const prev = state.project; if (prev.topic && prev.topic !== topic && prev.slides.some(s => s.approved) && !confirm('Replace this project topic and its slide drafts?')) return; state.project = newProject(topic, key); state.project.brand = { ...prev.brand }; state.project.generation = { ...prev.generation }; state.project.customTemplates = prev.customTemplates; state.index = 0; state.correction = ''; render(); scheduleSave(); }
async function generateDraft() {
  const topic = state.project.topic.trim(); if (!topic) return toast('Enter a carousel topic first.');
  state.busy = 'draft'; render();
  try {
    const { draft } = await api('/api/draft', { topic, notes: state.project.notes, clinic: { name: state.project.brand?.name, phone: state.project.brand?.phone }, provider: state.project.generation?.writingProvider, model: state.project.generation?.writingModel });
    state.project.slides = draft.slides; state.project.instagram = draft.instagram; state.project.youtubeTitle = draft.youtubeTitle; state.project.youtubeDescription = draft.youtubeDescription; state.project.stage = 1; state.index = 0; state.correction = ''; scheduleSave(); toast(`${state.status.textProviders?.[state.project.generation?.writingProvider]?.label || 'AI'} drafted five slides. Review and approve them one by one.`);
  } catch (err) { toast(`Could not draft: ${err.message}`); } finally { state.busy = ''; render(); }
}
async function rewriteSlide() {
  const correction = ($('#correction')?.value || '').trim(); if (!correction) return toast('Describe the change you want Codex to make.');
  state.busy = 'revise'; render();
  try { const { slide } = await api('/api/revise', { topic: state.project.topic, slide: current(), correction, clinic: { name: state.project.brand?.name, phone: state.project.brand?.phone }, provider: state.project.generation?.writingProvider, model: state.project.generation?.writingModel }); Object.assign(current(), slide, { approved: false, artwork: '', artworkProvider: '', artworkGeneratedAt: '' }); state.correction = ''; scheduleSave(); toast('Slide rewritten. Review the new text before approving.'); }
  catch (err) { toast(`Could not revise: ${err.message}`); } finally { state.busy = ''; render(); }
}
function approveSlide() { const s = current(); if (s.approved) s.approved = false; else { if (!s.heading.trim() || !s.body.trim()) return toast('Add a heading and body before approving.'); s.approved = true; if (state.index < 4) state.index++; } state.correction = ''; scheduleSave(); render(); }
async function referenceDataUrl() {
  const source = activeTemplate().img;
  if (/^data:image\//i.test(source)) return source;
  const response = await fetch(source); if (!response.ok) throw new Error('Could not load the selected template reference.');
  return optimizedImage(await response.blob(), 1280);
}
async function generateImage(index = state.index, batch = false) {
  const slide = state.project.slides[index];
  if (!slide.approved) { if (!batch) toast('Approve this slide before generating final artwork.'); return false; }
  if (!slide.visualPrompt?.trim()) { if (!batch) toast('Add a visual description for this slide first.'); return false; }
  if (!batch) { state.busy = 'image'; render(); }
  try {
    const provider = state.project.generation?.provider;
    const { image } = await api('/api/render-slide', { provider, model: state.project.generation?.model, slide, slideNumber: index + 1, brand: state.project.brand, referenceImage: await referenceDataUrl(), logoImage: state.project.brand?.logo || '' });
    Object.assign(slide, { artwork: image, artworkProvider: state.status.imageProviders?.[state.project.generation?.provider]?.label || state.project.generation?.provider, artworkGeneratedAt: new Date().toISOString() });
    scheduleSave(); if (!batch) toast('Final slide artwork generated. Verify every rendered word before export.'); return true;
  } catch (err) { toast(`Artwork generation failed: ${err.message}`); return false; }
  finally { if (!batch) { state.busy = ''; render(); } }
}
async function generateAll() {
  state.busy = 'all-images'; render();
  try {
    for (let i = 0; i < state.project.slides.length; i++) {
      if (!state.project.slides[i].artwork && !(await generateImage(i, true))) break;
    }
    scheduleSave(); toast(`Generated artwork: ${artworkCount()} / 5. Review each slide carefully.`);
  } finally { state.busy = ''; render(); }
}
function getSocialCopy() { const c = captionFallback(state.project); return { instagram: state.project.instagram || c.instagram, youtubeTitle: state.project.youtubeTitle || c.youtubeTitle, youtubeDescription: state.project.youtubeDescription || c.youtubeDescription }; }
async function downloadOne() { state.busy = 'export'; render(); try { downloadBlob(await finalArtworkBlob(current().artwork), `${String(state.index + 1).padStart(2, '0')}.png`); } catch (err) { toast(err.message); } finally { state.busy = ''; render(); } }
function folderName() { return (state.project.key || state.project.topic || 'dental-carousel').normalize('NFKD').replace(/[^a-zA-Z0-9-]/g, '-').replace(/-+/g, '-').replace(/^-|-$/g, '').slice(0, 45) || 'dental-carousel'; }
async function exportZip() {
  if (approvedCount() !== 5 || artworkCount() !== 5) return toast('Approve and generate final artwork for all five slides before export.');
  state.busy = 'export'; render();
  try {
    const dir = `smilecraft_${folderName()}`, entries = [];
    for (let i = 0; i < 5; i++) entries.push({ name: `${dir}/${String(i + 1).padStart(2, '0')}.png`, data: await finalArtworkBlob(state.project.slides[i].artwork) });
    const captions = getSocialCopy();
    if (state.includeCaptions) {
      entries.push({ name: `${dir}/instagram_caption.txt`, data: captions.instagram }, { name: `${dir}/youtube_title.txt`, data: captions.youtubeTitle }, { name: `${dir}/youtube_description.txt`, data: captions.youtubeDescription });
    }
    if (state.includeProject) entries.push({ name: `${dir}/project.json`, data: JSON.stringify({ ...state.project, ...captions }, null, 2) });
    entries.push({ name: `${dir}/README.txt`, data: 'Five AI-generated 1080x1350 PNG slides. Post 01.png through 05.png in order. Verify every rendered word, clinic detail, medical claim and visual before publishing. Project JSON can be imported from My Projects.\n' });
    downloadBlob(await makeZip(entries), `${dir}.zip`);
    await saveProject(); toast('ZIP downloaded with five separate PNGs and your selected deliverables.');
  } catch (err) { toast(`Could not export: ${err.message}`); } finally { state.busy = ''; render(); }
}
function invalidateArtwork(slides = state.project.slides) { for (const slide of slides) Object.assign(slide, { artwork: '', artworkProvider: '', artworkGeneratedAt: '' }); }
function normalizeGeneration(project, defaults) { const oldModels = project.models || {}; const generation = { ...defaults.generation, ...(project.generation || {}) }; if (!WRITING_MODELS[generation.writingProvider]) generation.writingProvider = defaults.generation.writingProvider; const writing = providerModels(generation.writingProvider); if (!generation.writingModel) generation.writingModel = oldModels.writing || defaults.generation.writingModel; if (!writing.some(([id]) => id === generation.writingModel)) generation.writingModel = writing[0]?.[0] || ''; if (!generation.model) generation.model = oldModels[generation.provider] || defaults.generation.model; if (!IMAGE_MODELS[generation.provider]) generation.provider = defaults.generation.provider; const images = providerModels(generation.provider, 'image'); if (!images.some(([id]) => id === generation.model)) generation.model = images[0]?.[0] || ''; return generation; }
async function openProject(id) { try { const { project } = await api(`/api/projects/${encodeURIComponent(id)}`); const defaults = newProject(); state.project = { ...defaults, ...project, brand: { ...defaults.brand, ...(project.brand || {}) }, generation: normalizeGeneration(project, defaults) }; delete state.project.models; state.project.slides = state.project.slides.map((slide, i) => ({ ...defaults.slides[i], ...slide, artwork: slide.artwork || '' })); state.index = 0; state.view = 'create'; state.correction = ''; render(); } catch (err) { toast(err.message); } }
function directUpdate(target) {
  const p = state.project, field = target.dataset.field, brand = target.dataset.brand, generation = target.dataset.generation;
  if (brand) { if (p.brand[brand] !== target.value) invalidateArtwork(); p.brand[brand] = target.value; if (brand === 'primary' || brand === 'accent') p.brand.paletteOverride = true; scheduleSave(); return; }
  if (generation) { p.generation ||= newProject().generation; p.generation[generation] = target.value; if (generation === 'provider') p.generation.model = providerModels(target.value, 'image')[0]?.[0] || ''; if (generation === 'writingProvider') p.generation.writingModel = providerModels(target.value)[0]?.[0] || ''; scheduleSave(); if (generation === 'provider' || generation === 'writingProvider') render(); return; }
  if (!field) return;
  if (field === 'topic' || field === 'notes' || ['instagram', 'youtubeTitle', 'youtubeDescription'].includes(field)) p[field] = target.value;
  else if (['heading', 'body', 'visualPrompt'].includes(field)) { current()[field] = target.value; Object.assign(current(), { approved: false, artwork: '', artworkProvider: '', artworkGeneratedAt: '' }); }
  scheduleSave();
}
async function onFileInput(target) {
  const file = target.files?.[0]; if (!file) return;
  try {
    const purpose = target.dataset.fileAction;
    if (purpose === 'import') {
      const p = JSON.parse(await file.text()); if (!p || !Array.isArray(p.slides) || p.slides.length !== 5 || !p.brand) throw new Error('Not a SmileCraft five-slide project.');
      const defaults = newProject(); state.project = { ...defaults, ...p, id: '', updatedAt: '', brand: { ...defaults.brand, ...(p.brand || {}) }, generation: normalizeGeneration(p, defaults) }; delete state.project.models; state.project.slides = state.project.slides.map((slide, i) => ({ ...defaults.slides[i], ...slide, artwork: slide.artwork || '' })); state.index = 0; state.view = 'create'; state.project.stage = 1; render(); await saveProject(true); return;
    }
    const image = await optimizedImage(file, purpose === 'logo' ? 400 : 1150);
    if (purpose === 'logo') { state.project.brand.logo = image; invalidateArtwork(); }
    if (purpose === 'template') {
      const id = `custom-${Date.now()}`; state.project.customTemplates ||= [];
      state.project.customTemplates.push({ id, name: file.name.replace(/\.[^.]+$/, '').slice(0, 35) || 'My reference', kind: 'Uploaded reference', img: image, dark: '#073a42', teal: '#16aba5', light: '#b5eeeb', layout: 'reference' }); state.project.template = id; invalidateArtwork();
    }
    scheduleSave(); render(); toast('Image added to this local project.');
  } catch (err) { toast(`Could not add file: ${err.message}`); }
}
async function onAction(node) {
  const action = node.dataset.action, value = node.dataset.value, i = Number(node.dataset.index), id = node.dataset.id;
  if (state.busy && !['nav', 'select-slide'].includes(action)) return;
  switch (action) {
    case 'noop': return;
    case 'nav': return showView(value);
    case 'new': { const previous = state.project; state.project = newProject(); state.project.brand = { ...previous.brand }; state.project.generation = { ...previous.generation }; state.index = 0; state.view = 'create'; return render(); }
    case 'stage': return gotoStage(i);
    case 'choose-topic': { const preset = TOPICS.find(t => t.key === value); return setTopic(preset?.topic || '', preset?.key || ''); }
    case 'draft': return generateDraft();
    case 'sample': if (!state.project.topic.trim()) return toast('Enter or choose a topic first.'); state.project.slides = starterSlides(state.project.topic); state.project.stage = 1; state.index = 0; scheduleSave(); toast('Editable starter loaded (not AI-generated). Review the medical copy and approve each slide.'); return render();
    case 'refresh-agy': return refreshAgyModels(true);
    case 'select-slide': state.index = i; state.correction = ''; return render();
    case 'approve': return approveSlide();
    case 'revise': return rewriteSlide();
    case 'choose-template': if (state.project.template !== value) invalidateArtwork(); state.project.template = value; scheduleSave(); if (state.view === 'templates') { state.view = 'create'; state.project.stage = 2; } return render();
    case 'generate-image': return generateImage();
    case 'generate-all': return generateAll();
    case 'clear-artwork': Object.assign(current(), { artwork: '', artworkProvider: '', artworkGeneratedAt: '' }); scheduleSave(); return render();
    case 'clear-logo': state.project.brand.logo = ''; invalidateArtwork(); scheduleSave(); return render();
    case 'save': return saveProject(true);
    case 'toggle-captions': state.includeCaptions = !state.includeCaptions; return render();
    case 'toggle-project': state.includeProject = !state.includeProject; return render();
    case 'caption-tab': state.captionTab = value; return render();
    case 'copy-caption': try { await navigator.clipboard.writeText(getSocialCopy()[state.captionTab]); toast('Caption copied to clipboard.'); } catch { toast('Clipboard unavailable. Select and copy the text manually.'); } return;
    case 'download-one': return downloadOne();
    case 'export': return exportZip();
    case 'open-project': return openProject(id);
    case 'delete-project': if (confirm('Delete this locally saved project? This cannot be undone.')) { try { await api(`/api/projects/${encodeURIComponent(id)}`, undefined, 'DELETE'); await listProjects(); render(); toast('Project deleted.'); } catch (err) { toast(err.message); } } return;
  }
}
el.addEventListener('click', event => { const node = event.target.closest('[data-action]'); if (node) { event.preventDefault(); onAction(node); } });
el.addEventListener('input', event => { if (event.target.id === 'correction') state.correction = event.target.value; else directUpdate(event.target); });
el.addEventListener('change', event => { if (event.target.dataset.fileAction) onFileInput(event.target); else if (event.target.dataset.generation) directUpdate(event.target); });
await listProjects();
try {
  state.status = await api('/api/status');
  if (state.status.textProviders?.antigravity?.available) await refreshAgyModels();
  const selected = state.project.generation?.provider;
  if (!state.status.imageProviders?.[selected]?.available) {
    const ready = ['openai', 'gemini', 'antigravity', 'codex'].find(id => state.status.imageProviders?.[id]?.available);
    if (ready) state.project.generation.provider = ready;
  }
} catch {}
render();
